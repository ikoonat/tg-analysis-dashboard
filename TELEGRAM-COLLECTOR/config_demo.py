"""Compatibility import for older demo commands.

The canonical configuration lives in config.py so demo and production paths
use the same language registry and asset paths.
"""

from config import Config